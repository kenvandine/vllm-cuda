name: build-vllm-cuda
description: >
  Installs the official vllm/torch wheels (unmodified, fat/multi-arch) for
  one host architecture, packages the result, and publishes it as a release
  asset. Shared by build-x64 and build-arm64 in build-release.yml -- see that
  file's header comment and the README for the full rationale, including why
  this ships one fat build per platform rather than per-GPU-arch builds.

inputs:
  version:
    required: true
    description: vllm version being built (e.g. 0.25.1)
  tag:
    required: true
    description: release tag (e.g. vllm0.25.1)
  asset_platform:
    required: true
    description: asset platform component, e.g. linux-x64 or linux-arm64
  pbs_triple:
    required: true
    description: python-build-standalone target triple, e.g. x86_64-unknown-linux-gnu
  pbs_py:
    required: true
    description: python-build-standalone CPython version, e.g. 3.12.13

runs:
  using: composite
  steps:
    - name: Download portable Python (python-build-standalone)
      shell: bash
      env:
        PBS_TRIPLE: ${{ inputs.pbs_triple }}
        PBS_PY: ${{ inputs.pbs_py }}
      run: |
        set -euo pipefail
        # vllm's official wheel is cp38-abi3 (works with any CPython >= 3.8),
        # so there's no channel-driven Python version split like vllm-rocm
        # needs -- pick one modern relocatable interpreter, per host arch.
        PBS_TAG=20260623
        PBS_URL="https://github.com/astral-sh/python-build-standalone/releases/download/${PBS_TAG}/cpython-${PBS_PY}+${PBS_TAG}-${PBS_TRIPLE}-install_only.tar.gz"
        ROOT="$RUNNER_TEMP/vllm-build/vllm"
        mkdir -p "$RUNNER_TEMP/vllm-build"
        curl -fsSL "$PBS_URL" | tar -xz -C "$RUNNER_TEMP/vllm-build"
        mv "$RUNNER_TEMP/vllm-build/python" "$ROOT"
        "$ROOT/bin/python3" --version
        echo "VLLM_ROOT=$ROOT" >> "$GITHUB_ENV"

    - name: Install vLLM + PyTorch from official PyPI (fat, all architectures)
      shell: bash
      env:
        VERSION: ${{ inputs.version }}
      run: |
        set -euo pipefail
        "$VLLM_ROOT/bin/python3" -m pip install --upgrade pip
        # Plain PyPI install -- no --index-url/--extra-index-url override.
        # PyPI's default linux wheel for torch (both x86_64 and aarch64) is
        # already CUDA-enabled and is what vllm's own dependency metadata
        # pins, so this is the simplest correct install (see README). pip
        # auto-selects the aarch64 wheel on an ARM64 interpreter.
        #
        # This wheel is left exactly as PyPI publishes it: every native cubin
        # torch/vllm ship for this host arch (sm_75/80/86/90/100/120 on
        # x86_64; sm_80/90/100/120 on arm64) plus PTX for forward
        # compatibility with newer GPUs. There is no per-GPU-arch pruning --
        # see the README for why (nvprune cannot process already-linked
        # shared libraries, only unlinked relocatable objects).
        "$VLLM_ROOT/bin/pip" install "vllm==${VERSION}"
        "$VLLM_ROOT/bin/python3" -c "import torch; print('torch', torch.__version__, 'cuda', torch.version.cuda)"

    - name: Add vllm-server launcher shim
      shell: bash
      run: |
        set -euo pipefail
        cat > "$VLLM_ROOT/bin/vllm-server" << 'LAUNCHER_EOF'
        #!/usr/bin/env bash
        set -euo pipefail
        SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
        exec "$SCRIPT_DIR/python3" -m vllm.entrypoints.openai.api_server "$@"
        LAUNCHER_EOF
        chmod +x "$VLLM_ROOT/bin/vllm-server"
        bash -n "$VLLM_ROOT/bin/vllm-server"

    - name: Smoke test (import)
      shell: bash
      run: |
        set -euo pipefail
        # Can't run `vllm-server --help`: `python -m ...api_server` sets
        # __name__ == "__main__", which executes api_server.py's
        # `if __name__ == "__main__":` block unconditionally (before
        # argparse even looks at --help). That block builds the full CLI
        # parser, which instantiates a default DeviceConfig() that
        # auto-detects the accelerator -- and fails hard with "Failed to
        # infer device type" on a GPU-less CI runner, since this is the
        # CUDA-targeted wheel (not the CPU-build vLLM needs for its CPU
        # fallback). A plain import only exercises import-time code
        # (verifies torch/vllm/CUDA shared libs all load) without needing
        # a live GPU.
        "$VLLM_ROOT/bin/python3" -c "import vllm.entrypoints.openai.api_server"

    - name: Package asset (tar.gz, split if over GitHub's 2 GiB limit)
      shell: bash
      env:
        TAG: ${{ inputs.tag }}
        ASSET_PLATFORM: ${{ inputs.asset_platform }}
      run: |
        set -euo pipefail
        BASE="vllm-server-${TAG}-${ASSET_PLATFORM}-cuda"
        TARBALL="${BASE}.tar.gz"
        tar -czf "${TARBALL}" -C "$(dirname "$VLLM_ROOT")" "$(basename "$VLLM_ROOT")"

        LIMIT=$((1900 * 1024 * 1024))   # 1900 MiB, comfortably under 2 GiB
        SIZE=$(wc -c < "${TARBALL}")
        : > uploads.txt
        if [ "${SIZE}" -gt "${LIMIT}" ]; then
          split -b "${LIMIT}" -d -a 2 "${TARBALL}" "${BASE}.rawpart"
          mapfile -t PARTS < <(ls "${BASE}".rawpart* | sort)
          N=${#PARTS[@]}
          NN=$(printf '%02d' "${N}")
          i=1
          for p in "${PARTS[@]}"; do
            II=$(printf '%02d' "${i}")
            mv "${p}" "${BASE}.part${II}-of-${NN}.tar.gz"
            echo "${BASE}.part${II}-of-${NN}.tar.gz" >> uploads.txt
            i=$((i + 1))
          done
          printf '%s\n' "${N}" > "${BASE}.partcount"
          echo "${BASE}.partcount" >> uploads.txt
          rm -f "${TARBALL}"
          echo "Split ${TARBALL} (${SIZE} bytes) into ${N} parts"
        else
          echo "${TARBALL}" >> uploads.txt
          echo "Single asset ${TARBALL} (${SIZE} bytes)"
        fi
        cat uploads.txt

    - name: Publish to release
      shell: bash
      env:
        GH_TOKEN: ${{ github.token }}
        TAG: ${{ inputs.tag }}
        VERSION: ${{ inputs.version }}
      run: |
        set -euo pipefail
        if ! gh release view "$TAG" --repo "${{ github.repository }}" >/dev/null 2>&1; then
          gh release create "$TAG" \
            --repo "${{ github.repository }}" \
            --title "vllm-server $TAG" \
            --notes "Self-contained vllm-server bundles (vllm ${VERSION}) for Lemonade's vllm CUDA backend: the unmodified, official PyPI vllm+torch wheels for each host architecture (linux-x64, linux-arm64), each supporting every GPU generation that wheel natively targets. Multi-GB bundles are split into .partNN-of-MM.tar.gz + .partcount." \
            || true
        fi
        while IFS= read -r f; do
          [ -n "$f" ] || continue
          gh release upload "$TAG" "$f" --repo "${{ github.repository }}" --clobber
        done < uploads.txt
